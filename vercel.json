{
  "//": "Skip Vercel builds for non-main branches to prevent rapid auto-deploy loops. Adjust branch name as needed.",
  "ignoredBuildStep": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then echo \"Skipping Vercel build for $VERCEL_GIT_COMMIT_REF\"; exit 0; fi"
}
